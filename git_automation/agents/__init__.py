"""Agentic workflow (Phase 3): Claude Agent SDK manager + sub-agents.

The manager reads a GitHub issue, decomposes it into tasks, and routes each to a
sub-agent via the skill tree. Depends on ``core``, ``skilltree``, Claude Agent SDK.
"""
