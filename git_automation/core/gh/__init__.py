"""GitHub operations via the ``gh`` CLI (and ``gh api`` for REST/GraphQL).

Reads/writes issues, repos, PRs, comments, labels, and notifications.
Reuses the existing ``gh auth`` session. Depends on the ``gh`` binary.
"""
