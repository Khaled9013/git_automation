"""Agent-capability graph (Phase 2): nodes = skills, edges = specialization.

Owns the capability graph; serves graph data to the UI and routing decisions to
``agents``. Depends on ``core.models``.
"""
